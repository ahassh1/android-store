import React from 'react';
import { Outlet } from 'react-router';

const Favorites = () => {
    return (
        <div>
            <h1>helloo </h1>
            <Outlet/> 
        </div>
    );
};

export default Favorites;